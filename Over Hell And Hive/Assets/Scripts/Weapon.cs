using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon : MonoBehaviour
{
    public string Name;
    public bool Ranged;//                                                                       AP instead of Armor Penetration, for ease of use
    public int Tier, SlashOffence, PierceOffence, CrushOffence, CritMultiplier, Weight, Reach, APvalue;
    public Sprite WeaponIcon;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
